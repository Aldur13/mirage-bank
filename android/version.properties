# Single source of truth for app version. Bump before every release.
# versionCode is derived deterministically in app/build.gradle.kts as
# major*10000 + minor*100 + patch, so it never drifts from versionName.
VERSION_NAME=1.0.0
